#include <stdio.h>
#include "main.h"
/**
 * main - Prints it's  name.
 * @argc: Number of  arguments
 * @argv: Array.
 * Return: 0
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc * 0]);

	return (0);
}
